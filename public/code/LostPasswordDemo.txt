<template>
[...]
          <LostPasswordForm v-if="!submited" class="app-form" v-model="formData" @submit="sendFormToServer"/>
          <VerticalLayout v-if="submited">
            Those form data have been sent to the server
            {{ formData }}
          </VerticalLayout>
[...]
</template>

<script setup>
import {
  HorizontalLayout,
  Card, VerticalLayout,
} from "vue-component-toolkit/dist/vue-component-toolkit.js";
import CodeFragment from "@/components/CodeFragment.vue";
import DemoTemplate from "@/DemoTemplate.vue";
import {ref} from "vue";
import {useRoute} from "vue-router";
import LostPasswordForm from "@/demo/authentication/LostPasswordForm.vue";

const route = useRoute();
const validation = ref(null);
const error = ref(null);
const submited = ref(false);
const formData = ref({email: ''});

const sendFormToServer = (data) => {
  submited.value = true;
}

</script>
<style scoped>
.app-form {
  min-width: 450px;
}
</style>
